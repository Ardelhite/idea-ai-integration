.gradle/
build/
out/
*.log

# IntelliJ Platform Gradle plugin caches / Kotlin daemon state
.intellijPlatform/
.kotlin/

# IntelliJ project files (keep .idea out of VCS for this template)
.idea/

# Claude Code local (machine-specific) settings
.claude/settings.local.json

# Plugin signing keys / Marketplace secrets — never commit
*.pem
*.crt
*.p12
chain.crt
private.pem
secrets.properties

# Local release runbook (may contain key paths / personal notes)
howToUpload.md

# OS
.DS_Store
