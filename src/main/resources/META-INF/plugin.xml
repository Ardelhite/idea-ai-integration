<idea-plugin>
    <id>ai.sanakan.fugu.idea</id>
    <name>Karato - Agent for Sakana AI fugu</name>
    <vendor email="harima42@gmail.com">Karato</vendor>

    <description><![CDATA[
        <p><b>Karato</b> — an IntelliJ agent for <b>Sakana AI Fugu</b> (and <b>fugu-ultra</b>).</p>
        <p>Drives the Fugu CLI as a subprocess over a streaming-JSON protocol — chat with
        the agent, watch it edit files in your project, and review changes inline.
        Inspired by the architecture of open-source Claude Code GUIs.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Application-level settings -->
        <applicationService serviceImplementation="ai.sanakan.fugu.settings.FuguSettings"/>
        <applicationConfigurable
                parentId="tools"
                instance="ai.sanakan.fugu.settings.FuguConfigurable"
                id="ai.sanakan.fugu.settings.FuguConfigurable"
                displayName="Karato"/>

        <!-- Per-project chat-tab manager (owns and persists all FuguSessions) -->
        <projectService serviceImplementation="ai.sanakan.fugu.core.FuguSessionManager"/>

        <!-- Chat tool window -->
        <toolWindow
                id="Karato"
                anchor="right"
                icon="AllIcons.Toolwindows.ToolWindowMessages"
                factoryClass="ai.sanakan.fugu.ui.FuguToolWindowFactory"/>

        <!-- Open the Karato tool window by default when a project loads. -->
        <postStartupActivity implementation="ai.sanakan.fugu.ui.KaratoStartupActivity"/>
    </extensions>

    <applicationListeners/>
</idea-plugin>
